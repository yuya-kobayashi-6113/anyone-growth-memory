> **このファイルは Claude Code が毎回読む索引です。**
> 記憶の正本はこのリポジトリ (`anyone-growth-memory`) です。
> トピック別の詳細は `memory/` の各ファイルにあり、**必要になった時点で読みます**。
> 設計文書(`design-docs/`)とセッション記録(`session-docs/`)は Cowork のプロジェクトドキュメントに残してあり、そちらを参照します。
> 記憶を更新したら、同じターンで該当ファイルを直してコミットします。

---

## いまの状態【2026-08-23 時点】

**オープンPRの本数・有無はここに書かない。必ず GitHub で数え直す。**
記憶に書いてある状態は書かれた時点のもの。**報告や計画の根拠にする前に、その場で一次情報を取り直す。**

### 【重要】GitHub Actions が 2026-08-19 から一度も動いていない(2026-08-23 判明)

**両リポジトリのCIは、2026-08-19 を最後に一度も実行されていない。**すべてのジョブが4〜6秒で失敗しており、これはテストの失敗ではなく**ジョブが起動すらしていない**状態。

```
The job was not started because recent account payments have failed
or your spending limit needs to be increased.
Please check the 'Billing & plans' section in your settings
```

- **最後に本当に走ったCI**: ID基盤は 2026-08-19 04:48(411秒)、Growth は 2026-08-19 02:03(147秒)。**正常時の所要時間は ID基盤で約7分、Growth で約2.5分**。この長さが出ていない実行は「走っていない」と判断してよい
- **原因は GitHub アカウントの支払い失敗か使用量上限**(`allsmile-corp` の Billing & plans)。**リポジトリ側の設定やコードの問題ではない**
- **帰結: 2026-08-19 以降にマージされた変更は、どれもCIの検証を受けていない。**ID基盤は PR #68 以降、Growth は PR #9 以降がこれにあたる
- **ただし検証がゼロだったわけではない。**この期間のPRは「クラウド検証環境で検証コマンド一式が緑」を確認して提出している。**CIという二重目だけが失われていた**
- **「CIが緑になったことを確認してからレビュー依頼」という進め方は、この期間まったく機能していなかった。**PR本文に「CI緑」と書いたものがあれば、それは事実に反する

**判断: 当面はローカルのテストで賄う(2026-08-23 小林さん)。**リリース前の初期開発フェーズであり、CIの復旧を待って手を止める段階ではないため。

- **その結果、`verify.sh` が唯一の検証の関門になる。**CIが動いていた頃は二重だったものが一重になっている。**通さずにpushする回避をしない**
- **【見直す引き金】**: ①リリースが近づいたとき ②開発に人が増えたとき(自分以外の変更を機械で受け止める必要が出る) ③支払いを直したとき。**いずれかが来たらCIを復旧し、二重に戻す**
- 復旧するときは、両リポジトリの main でCIを1回流して緑を確かめる。あわせて**この期間にマージした変更のうち、ローカル検証の記録が残っていないものを洗い出す**

**教訓: 「CIが失敗している」を「テストが落ちた」と読まない。**所要時間が極端に短い失敗は、ジョブが起動していない可能性を先に疑う。

### 2026-08-23 の状態(Mac のローカル環境)

- **ローカルの `id-platform` を `main` へ同期した**(それまで origin が消えた `docs/reorganize-design-and-ops` に居て47コミット遅れていた)。**追跡先が消えたブランチが26本残っている**ので掃除がいる
- **Mac に必要なものは揃っている**(2026-08-23 実測): .NET 10.0.301 / Node 24 / pnpm 10.15.1 / python3 3.13.2 / Docker 27.4.0(デーモン稼働) / gh 2.83.2。**ID基盤の結合テストは Testcontainers なので Docker が要る。動く状態にある**
- **判断が4件おりた(2026-08-23 小林さん)**: ㉔はユニバーサルリンク方式で確定 / ⑬は推奨どおり案B / 旧HTML3本は廃止 / `PlaceholderTests.cs` は削除。**設計文書の正本二重(リポジトリ `docs/` と Cowork `design-docs/`)は当面いじらない**
- **`PlaceholderTests.cs` の削除は、承認した時点ですでに済んでいた**(2026-08-23 実測)。ID基盤リポジトリの `tests/IdPlatform.UnitTests/` にあったもので、`chore/remove-placeholder-test` が main にマージ済み。**Growth側を探しても見つからず「存在しない」と誤って結論づけかけた。**承認済みリストの項目は、着手前にどちらのリポジトリの話かを確かめ、コミット履歴で対応済みかを見る
- **税区分を見積の版の属性へ移した(2026-08-23 マージ済み)。**帳票の出力設定から外し、誰が出力しても同じ見積書になる状態にした。判断の正は Growth リポジトリの `docs/adr/0002-estimate-tax-mode-attribute.md`。**この作業中に、画面と帳票で合計の集計基準が違う既存の不整合が見つかっている**(→ [Growth帳票出力](memory/growth-documents.md))
- **㉔(モバイルの戻り方式)と⑬(サービス一覧の正本統一)をマージした(2026-08-24 JST)。**どちらもID基盤。㉔の判断の正は ID基盤の `docs/adr/0023-mobile-universal-links.md`。⑬は基盤APIに目録(`GET /v1/services`)を新設し、コンソールの環境変数 `ID_PLATFORM_CONSOLE_SERVICES` を廃止した
  - **⑬で分かったこと: 接続サービスの並び順の正本は、これまでどこにも無かった。**目録の並び順項目で初めてできた
  - **未決4件は「実装は済んでいて、値がまだ決まっていない」もの。**①NBSSモバイルの実ドメイン(いまは例示専用ドメインの仮値。ステージングへレルム設定を適用する時点まで) ②各環境への環境変数の注入先(環境構築フェーズ着手時) ③各サービスの入口URLの実値(**空のままだと「サービスを開く」リンクが1つも出ない**) ④`growth` の並び順の正式な値
  - **ID基盤の検証はMacで2分強**(2026-08-23 実測、Testcontainersで結合テストを実際に起動した状態)。Growthの1分強より重い。**`dotnet format --verify-no-changes` が入っているので書式ずれだけでも落ちる**

- **全体コードレビューを行い、判断が要らない指摘だけを PR #26 としてマージした(2026-08-24 05:06 JST、`ee5b5ab`)。**読み取り専用エージェント4体(バックエンド設計ルール適合 / フロントエンド設計ルール適合 / テストの穴 / 設計書と実装のずれ)で洗い出し、実装は Codex、検証は Claude が独立に実施。**最も重いものは「削除済みの見積の版から帳票を生成できてしまう」**(見積の削除が版へ伝播せず、帳票の読み出し側が親の見積を見ていなかった)
  - **設計判断が要るもの・業務確認が要るものは一切直していない。その一覧と、直さなかった理由は PR #26 の本文にある。****ここに書き写さない**(二重管理になる)。判断がおりたものから順に別PRへ
  - **このレビューで、既知課題の1件(404の文言)の原因箇所が特定できた**(→ 下の「判断待ち・検討課題」の1番)

- **ルート不在の404に専用エラーコードを割り当てた(2026-08-24 マージ済み、PR #27、`0b4e2c3`)。**判断待ち1番の解消。**この作業で、Codexの実行環境の制約が1つ分かった**(→ 下の「技術上の落とし穴」)

- **ID基盤の画面フィードバック17件へまとめて対応し、PR #82 がマージされた(2026-08-25)。**1PR構成は小林さんの判断。組織の並び順は ADR-0024。**未ログイン時の戻るリンクは、入口オリジンだけ返す匿名API(`/v1/services/entry-origins`)の新設で解消(小林さん承認)。**実機での見た目確認(accountTheme・ツリー行高・省略表示)は未実施のまま(PR本文に記載)。**GrowthのLPへ returnUrl を付ける変更はGrowth側で対応中**
  - **FBの原因調査と対応方針の記録は、`anyone-growth` ワークツリーの `test-results/review/id-platform-feedback-plan.html`**(gitignore配下・タブ17件)。決定の経緯はここが最新

- **実機確認の指摘対応として PR #83 を提出した(2026-08-25、レビュー待ち)。**①組織の編集欄が選択切替で前の値のまま残る不具合(そのまま保存すると別組織を改名できた)を修正 ②**「認証製品(Keycloak)の名称・ロゴは利用者に見せない」方針を実装・設計文書へ明文化**(アカウント管理画面のロゴ・タブ名・文言を差し替え。ログイン画面とメールは露出なしを実測済み)。**この方針はADR-0017(パスワード変更は認証製品の画面に任せる)と両立**: 画面のホスト先は変えず、ブランドの露出だけを消す

### 2026-08-22 に進んだこと(このセッションの外で実施。詳細は各リポジトリのコミットを見る)

- **anyone-growth**: 帳票テンプレート・生成物の削除でファイル実体を残す / 期限切れを掃除する常駐処理 / **ADRを新設し帳票出力の詳細設計書を追加**
- **anyone-user-platform**: **設計正本・ADR・運用手順をリポジトリへ集約**し、索引と印刷CSSを整備

### 両リポジトリの現在の形

- ルートに **`CLAUDE.md` と `AGENTS.md`** がある
- **`docs/{adr,design,rules}`** がある(ID基盤はさらに `integration` / `operations`)。`docs/rules/` にはコーディング規約が5本
- **設計文書のリポジトリ集約が進んでいる。** Cowork の `design-docs/` との役割分担は**未整理**。どちらが正かを決める必要がある

### 開発環境の移行【2026-08-23 時点・進行中】

- **記憶の正本はこのリポジトリ。** Cowork の `project-memory/` は移設時点の写しで、更新しない
- **実装・テスト・push を Mac へ移す方針で決定。** 計画の正本は Cowork の `session-docs/local-migration-plan-20260822.md`
- **ガードレール(`.claude/`)は両リポジトリの main に入った**(2026-08-22 マージ済み)。下の「ガードレールの作法」はもう有効
- **Mac側の残作業**は Cowork の `session-docs/mac-setup-steps-20260822.md`。手順1(記憶を読ませる)・手順2(PRマージ)・**手順4(通し確認)は完了**。**残るのは手順3(運用スキルの持ち込み)**
- **手順4は2026-08-23に完了した。**ガードレールを実地で確認済み。証跡が無い状態と証跡が古い状態のどちらでも push とPR作成は止まり、検査を飛ばすコミット、lease なしの強制push、リモートに合わせる強制リセット、stash とコミットを繋げた形も止まった。lease つきの強制pushは通る
- **確認の途中で、フックが自分自身の確認コマンドを止めた。**フックはコマンドの文字列を見て判定するので、**危険なコマンドを文書に書き写そうとしただけでも止まる**。回避せず、文書側をコマンドの形で書かない表現に変えるのが正しい対処
- **Mac での `pnpm run ci` の所要時間は1分強**(2026-08-23 実測、結合テストを実際に走らせた状態)。**CIが正常だった頃のGrowthの所要時間と同程度**なので、CIが止まっている間もローカルで同等の検証ができる
- **ローカルで結合テストを実際に走らせるには、CIと同じ接続情報のPostgreSQLを用意して `GROWTH_TEST_DATABASE` を渡す。**渡さないと `PostgreSqlFactAttribute` が黙ってスキップし、**緑に見えても結合テストは1本も走っていない**。ID基盤(Testcontainers方式)とは仕組みが違う
  - **その用途のコンテナは Mac 上に常駐している。**`growth-verify-db`(`postgres:17-alpine`、CI と同じ `growth_ci`/`growth`/`growth`)。**ポートは 5432 ではない。**ID基盤の PostgreSQL が 5432 を使っているため別ポートに逃がしてある。**実際のポートは `docker ps` で確かめる**
  - **スキップされた本数は「合計」と「スキップ」の両方を見ないと気づけない。**`dotnet test` の要約は、全部スキップでも「成功!」と出る
- **Codeタブ(GUI)から起動する場合、`--add-dir` も `~/.zshrc` の環境変数も使えない。****そのリポジトリの `.claude/settings.local.json`** に `permissions.additionalDirectories` と `env` を書く。**【未了】いまは `~/.claude/settings.json`(ユーザー設定)に書いたままで、案件を増やすと関係ない記憶まで毎回読み込まれる。**ユーザー設定から外して各リポジトリへ移す作業が残っている(**`env` キーが `settings.local.json` から効くかは未確認**。効かなければ環境変数だけユーザー設定に残す)。**親フォルダをまとめて開くとガードレールが効かなくなる**ので、1リポジトリ1セッションにする
- **コードリポジトリへの書き込みは Mac から行う。**Coworkから書いてよいのは記憶リポジトリと運用リポジトリだけ。**やむを得ずCoworkから書くときは ①blob SHA照合 ②テスト未実行をPR本文に明記 ③人のレビューを通す** の3点を満たす(2026-08-23 立て直し。理由は文字化けではなく**検証の迂回**)

### ガードレールが入ったあとの作法

- **push と PR作成の前に `bash .claude/hooks/verify.sh` を実行する。** テストが緑のときだけ証跡が残り、**証跡が無い/古いと push と `gh pr create` が止まる**
- **`--no-verify`、`git commit -n`、lease なしの force push は止まる**
- 検証コマンドは `.claude/verify-command`(Growth は `pnpm run ci`、ID基盤は CI と同じ6コマンドの並び)
- **python3 が要る。**無いとフックは素通しせず停止する

### 決着した点(2026-08-22)

**ID基盤の結合テストはCIで走っている。DBの用意の仕方が2リポジトリで違うだけ。**

- **ID基盤は Testcontainers 方式。**テスト側が `postgres:17` / Keycloak / Mailpit を自前起動する(`PostgreSqlBuilder` を `IAsyncLifetime` で起動)。接続文字列は `GetConnectionString()` から取り、**環境変数も設定ファイルも読まない**。だからCIに `services:` が無いのが正しい
- **Growth は CI がサービスコンテナを立てる方式**(`postgres:17-alpine` + `GROWTH_TEST_DATABASE`)
- **静かにスキップされる経路は無い。**Dockerが使えなければ `StartAsync()` が例外を投げ、Skip ではなく **fail** する。**「CI緑」の意味は壊れていない**
- **手元で `dotnet test` を走らせるにも Docker が要る**(ID基盤)

**`_to_delete/` は削除済み**(2026-08-22)。

### 教訓: CIの設定を変えたら、テストのコメントとREADMEも直す

2026-08-12 にCIから `--filter FullyQualifiedName!~IntegrationTests` を外したとき、**「CIでは当面除外中」というコメントが32ファイルに残った。**その結果、**私も別セッションのClaudeも「結合テストが走っていない」と誤診した。**2026-08-22 に全件直したが、**同じ形の陳腐化は繰り返し起きる**。

- **`search_code` はこの2リポジトリを索引しておらず0件を返す。**網羅性の確認は**手元で `grep -rn` を叩く**のが確実
- **設定の変更と、それを説明している文章の修正は、同じPRに入れる**

---

> **進め方の共通ルールは allsmile-ops スキルが正**(dev-workflow / memory-ops / proposal-format / no-sycophancy / deliverable-style / env-pitfalls / docs-backup / ops-maintenance / project-bootstrap / subagent-instructions)。
> ここに置くのは**案件固有の情報だけ**。共通ルールをこちらに書き写さない(2026-08-07 移行)。
> **PR番号・テスト件数・オープンPRの有無は書かない。**数え方は [github-repos](memory/repos.md) の表を見て数え直す(2026-08-09)。

- [体制と前提](memory/team.md) — allsmile開発→エニワン納品。意思決定者=小林さん(PMメイン・技術3年)。スマホで設計HTMLを見る
- [ID基盤の設計・状況](memory/id-platform.md) — フェーズ1。**画面はNext.jsへ全面移行済み・接続サービス連携は二者同意で稼働**。判断とその理由の正。正本の階層もここ
- [Growth見積の設計・状況](memory/growth.md) — フェーズ2。**見積の永続化と契約の開通まで到達**。ID基盤との連携の作りはここ
- [Growth帳票出力の設計・状況](memory/growth-documents.md) — **見積書のExcel出力(2026-08-20 実装しPR提出)**。Aspose.Cellsの罠、テンプレート記法と検査、出力設定の増やし方はここ
- [GitHubリポジトリ](memory/repos.md) — anyone-user-platform / anyone-growth。**ローカル起動は `tools/dev/devenv.sh`**、進捗の数え方、CI事情(基盤とGrowthで別)
- [クラウド検証環境](memory/verification-env.md) — **クラウド側で全スタックを組み立てて実ブラウザで確認する手順**。遮断の回避経路。2026-08-18 確立
- [用語統一](memory/terminology.md) — 会社=Organization、組織=OrgUnit。**2026-08-17にOrgUnitの日本語を「部署」から「組織」へ統一した**。ADRは旧表記のまま残るので読み替える
- [技術スタック](memory/tech-stack.md) — C#/.NET 10 + TypeScript/Next.js、**pnpm 10.15.1に統一**、EC2+Docker、RDS(PostgreSQL)、Keycloak、SES、Caddy

## 最初に読む: 2026-08-17〜19 に前提が7つ変わった
**この期間に大量の実装が入り、それ以前のメモを誤読しかねない変化がある。**

1. **画面はRazor Pagesではない。Next.jsに全面移行した。** `src/IdPlatform.Web` はプロジェクトごと削除済み。C#のホストは `src/IdPlatform.Api` 1本。画面は `apps/id-platform-console`(ADR-0013 + ADR-0015)
2. **「NBSSの協力業者関係 = guest所属」は撤回された**(ADR-0007)。Membershipは自社メンバーにだけ作る。職人・協力業者は接続サービス側が持つ。代わりの経路が Sponsored Enrollment
3. **用語が変わった。OrgUnitの日本語は「部署」ではなく「組織」。**会社=Organization は変わらない
4. **パッケージマネージャーは pnpm に統一。**基盤も npm workspaces から移行した。**ローカル起動は `bash tools/dev/devenv.sh up`**
5. **「テストはローカル実行が唯一の関門」は、もう正しくない。**クラウド側で**結合テストを含む全件**と**実ブラウザでの通し確認**ができる(→[クラウド検証環境](memory/verification-env.md))
6. **サインアップの流れが変わった(2026-08-19、⑫でマージ済み)。**入口は**メールアドレスだけ**。氏名とパスワードは**確認メールのリンクを開いた先**で決める。**メールは1通**。「パスワード設定メールを受け取る」ボタンと Keycloak の「次の操作を実行します」画面は**もう存在しない**
7. **ログイン名はメールアドレスそのもの(2026-08-19、⑮でマージ済み・ADR-0017)。**レルム設定を `registrationEmailAsUsername: true` に変えたので、認証サービス側がメールアドレスからログイン名を作る。**基盤アプリはログイン名を送らない。**メールアドレスを変えるとログイン名も追従する

## クラウドで .NET 10 SDK を入れる最短経路【2026-08-20 追加】
**Ubuntu 24.04 の標準リポジトリに `dotnet-sdk-10.0` がある。**`apt-get update && apt-get install -y dotnet-sdk-10.0` で入る(実測 10.0.111)。
Microsoft の配布ホスト(`dot.net` / `builds.dotnet.microsoft.com` / `packages.microsoft.com` / `aka.ms`)は**すべて遮断**されたままなので、
[クラウド検証環境](memory/verification-env.md) に書いてある「Docker Hub の bitnami/dotnet-sdk から取り出す」回避は**もう要らない**。

## 進め方【2026-08-18 決定】
**PRを出す前に、①差分が指示どおりかを自分で確認 → ②ビルドとテストを実際に走らせて緑を確認 → ③結果を添えてPR。**
CIの結果を待つ前に、クラウド検証環境で通す。

## 日付は `date` のローカル時刻で書く。`date -u` ではない【2026-08-24 追加】

**このリポジトリの日付表記は JST。**`date -u`(UTC)を見て記録すると、日本時間の朝5時までは1日前になる。

- **実際に、環境表示の日付を「1日ずれている」と誤って自己申告した**(2026-08-24 未明)。UTCは 08-23、JSTは 08-24 で、環境表示のほうが正しかった
- **`date` と `date -u` の両方を出して、どちらで書いているかを意識する。**memory-ops の鉄則3は「環境表示ではなく `date` の実測値」であって、UTCで書けとは言っていない

## Codexの環境ではHTTPテストが動かない【2026-08-24 実測】

**Codexのサンドボックスでは ASP.NET Core のインメモリホスト(`WebApplicationFactory`)が起動できない。**`Timed out waiting for the entry point to build the IHost after 00:05:00` で、HTTPテストが軒並み失敗する。

- 実例: Codexが**「24件失敗」と報告したが、実装は正しく、Claudeの環境では全緑だった**
- **`Growth.Api.Tests` のHTTPテストはCodexに走らせても意味がない。**指示書に「テストを実行しないこと」と書き、**検証はClaudeが引き受ける**
- **DBを使うリポジトリのテストと、フロントのテスト、ビルドはCodex側でも通る。**動かないのはホストを起動するものだけ

## 技術上の落とし穴【実際に踏んだもの】

**CSPの `form-action` は、フォームの送信先だけでなく「送信後の遷移先」にも効く**(2026-08-18)。
外部のIdP(Keycloak)へ遷移する画面では毎回ここが罠になる。**基盤・Growthの両方で踏んだ。**

- ボタンを**リンク(GET)からフォーム(POST)に変える改修は、CSPの見直しとセット**で行う
- Issuerのオリジンは**ハードコードせず設定から解決**して `form-action` に許可する(両リポジトリとも同じ方式に揃っている)
- **サーバーの応答が正しくてもブラウザが拒否する**ため、単体テストでは捕まらない。実ブラウザでの通し確認が要る

**Keycloakの「パスワード設定」は、ログインの一部かどうかで挙動が変わる**(2026-08-19 実測)。

- `execute-actions-email` で送ると、**中継画面「次の操作を実行します」が必ず挟まる**。`redirect_uri` を渡しても消えない。完了後も自動で戻らず、**ログイン済みにもならない**
- 一方、**認可リクエストの中から入るパスワード再設定**なら中継画面が出ず、決め終わるとそのままアプリへ戻り、**ログインまで成立する**(認可コードが返ることを確認)
- **ただし後者に自前の登録処理(氏名の受け取り・規約同意・基盤DBの状態変更)を差し込む標準的な手段はない。**⑫では**ここで断念し、ログイン画面を1枚はさむ形に落とした**。同じ発想を再び持ち出すときはこの制約から確認する
- **パスワードを基盤が受け取って認証サービスへ設定する経路は自前で持っている**(`IInitialPasswordProvisioner`)。自己登録と接続サービス経由の仮登録で共用。**パスワードはDB・outbox・監査ログのどこにも保存しない**

**Keycloakのログイン名は、メールアドレスと必ず対で考える**(2026-08-19 実測・ADR-0017)。

- レルムが `editUsernameAllowed: false` だと、管理APIで**ログイン名を含めて更新すると400で弾かれる**(`error-user-attribute-read-only`)
- かといって**メールアドレスだけ更新すると204で通ってしまい、ログイン名が古いまま残る**。古いアドレスでログインできる状態が残り、後でそのアドレスを他人が使うとログイン名の重複で詰まる。**成功応答が返るので気づけない**
- 採った対処は**レルム設定 `registrationEmailAsUsername: true`**。ログイン名は認証サービスが作るので、アプリは**メールアドレスだけを送る**
- **レルム設定の変更は環境ごとに keycloak-config-cli の適用が要る。**アプリのデプロイだけだとメールアドレス変更が失敗する

**接続サービスへの新しいお知らせを足すときは、変更フィードの該当箇所をすべて直す**(2026-08-19、⑯⑰で実測)。**契約のお知らせは7か所、会社のお知らせは4か所**と系統で数が違う。**既存の同系統のお知らせを全文検索して、出てくる場所をすべて直すのが確実。**さらに**会社の削除のお知らせだけは、配信の窓の外でも届ける特例が要る**(削除と同時に契約を解約するため、窓の中だけで判定すると誰にも届かない)。

**外部サービスへ戻す仕組みは既にある。**許可したオリジンだけを受け付ける実装が `sanitizeExternalReturnUrl` にある。**戻り先を扱うときは必ずこれを通す**(2026-08-19 に「Growthへ戻る」導線で利用)。表示名は**基盤側の設定から引き、外部から渡された文字列を画面に出さない**。

**認証サービスの状態を変える操作は、先に認証サービスを呼び、成功してから基盤の状態を変える**(2026-08-19、⑮で確立。⑫と同じ形)。トークン(1回だけ使えるリンク)も**認証サービスの呼び出しが成功するまで使い切らない**。順序を逆にすると、認証サービス側が失敗したときに**同じリンクでやり直せなくなる**。

**接続サービスの情報は3か所に分かれている**(2026-08-19 調査。詳細は ⑬提案書)。

- コンソールの環境変数 `ID_PLATFORM_CONSOLE_SERVICES`(コード・表示名)。**「画面の並びの正本」と書いていたのは誤り**(2026-08-23 実測)。**そもそもこの環境変数はリポジトリのどこにも設定されておらず、効いていたのは読み込み処理の中に書かれた既定値だった。**環境変数を設定した場合だけ最後にコードの辞書順へ並べ替える分岐があり、既定値にはそれも掛からない。**つまり並び順の正本はどこにも無く、コードに書いた順がそのまま出ていただけ**
  - **同じ箇所を2度誤った。**1度目は「環境変数が並び順の正本」、2度目は「読み込み処理が辞書順に並べ替えている」。どちらも**設定の宣言だけを見て、その値が実際にどう使われるかを最後まで追わなかった**のが原因。**設定項目について書くときは、読み込み処理・既定値・分岐を最後まで読む**
- 基盤APIの設定 `Users:SponsoredEnrollment:Services`(コード・**表示名**・有効/無効・仮登録の戻り先URL)
- DB `orgs.service_clients`(コード・種別・状態。**正はコード管理の設定で、DBは写し**)
- **表示名が二重。どこにも「サービスの入口URL」が無い**ため、「利用サービス」画面から各サービスを開くリンクが作れない

**認知用HTMLに実物のスクリーンショットを載せるとき**(2026-08-19)。

- 開発ビルドの画面には**左下にNext.jsの開発ツールのバッジ**が写り込む。撮影時に `nextjs-portal{display:none}` を差し込むか、後から画像処理で消す
- 画像はJPEG化して横1000px程度に落とすと、12枚で**HTML全体が約470KB**に収まる
- **base64をチャットに載せずに済ませる**: ビルド用スクリプトでファイルから読み込んでHTMLを組み立て、`project_write` の `local_path` でアップロードする。**作業ディレクトリの中に置かないと弾かれる**

**クラウドから GitHub リポジトリを clone できない**(2026-08-21 実測)。`GH_TOKEN` は環境変数にあるが、`https://x-access-token:$GH_TOKEN@github.com/...` での clone は「Invalid username or token」で失敗する。`gh` コマンドも入っていない。**コードを読むときは GitHub MCP の `get_file_contents` でディレクトリを辿る。**`search_code` は **anyone-growth も索引しておらず0件を返す**(基盤リポジトリと同じ)。**横断的な洗い出しはサブエージェントに `get_file_contents` で辿らせる。**

## 次にやること

### ㉓ NBSSからの「認証情報の平文コミット」報告への対応 → **対応不要でクローズ(2026-08-21 小林さん判断)**
計画書 `session-docs/growth-fix-20260821-credential-leak-response.md`。**値は文書にもメモリにも書かない。**

- 報告内容: `apps/backend/nuget.config`(GitHub PAT)と `apps/backend/Projects/WebAPI/Properties/launchSettings.json`(AWSキー・SMTPパスワード)が平文コミット。要ローテーション
- **調査結果1: 報告の2ファイルは `allsmile-corp` の全12リポジトリのどこにも無い。**`apps/backend` というパス自体が存在しない。**NBSS側の別リポジトリ(別Organizationか別ホスティング)。まずどのリポジトリか確認を戻す**
- **調査結果2: 自社2リポジトリ(ID基盤・Growth)に本物の平文コミットはゼロ。**`nuget.config` は両方とも存在しない。`launchSettings.json` は各1本で起動URLと環境名だけ。**AWS・SMTP・PAT・Asposeライセンスのいずれも混入なし**。`.gitignore` も整備済み
- **私たちへの影響は「共有基盤かどうか」次第。**失効の前に3点を確認する: ①AWSキーは同じアカウントか(EC2/RDS/SES/**S3=帳票の保存先**が止まる) ②SMTPはSESの同じ送信ユーザーか(**確認メールが止まりサインアップ不可**) ③PATは `allsmile-corp` に権限を持っていたか。**自社CIはPATを使っていないので失効でCIは止まらない**
- **報告の対応案①②③の順序は妥当。ただし「その鍵が実際に不正利用されていないか」の確認が抜けている**(CloudTrail / GitHub組織の監査ログ / SESの送信履歴)。**失効と並行して履歴を保全する**
- **提案した3点(NBSSへの確認 / CIでの混入検知(gitleaks等) / 開発用の暗号鍵とサービス間シークレットの注入経路の確認)は、すべて見送り。**「認証の方は対応不要」(2026-08-21)
- **【見直す引き金】次が起きたらこの記録を読み直す**: ①AWSのデプロイ・S3(帳票の保存先)・SESのメール送信が突然失敗する ②確認メールが届かずサインアップが通らない ③リポジトリに身に覚えのないアクセス・変更の記録。**いずれもNBSS側が共有の鍵を失効させた場合に起きる形で、症状から原因にたどり着きにくい**
- **本番運用の前の点検では**、Growth開発用設定の `SensitiveDataProtection` の鍵と `IdPlatform:Directory:ClientSecret` を見る(**ID基盤側と同じ文字列体系を共有**しており、環境ごとの上書きが漏れると全環境で同一値になる構造)

### ⑯⑲⑳㉑㉒ → **すべてマージ済み**(2026-08-21〜22)

経過は Cowork の `session-docs/` と各リポジトリのADRにある。**記憶に残すのは、後で効く判断だけ。**

- **㉑ Growth独自の権限機能は停止した。**判定を1箇所にまとめて常に許可を返す形。**DBテーブルは残すが使わない。要件が決まったらフラグを戻すのではなく作り直す。**止めたあとは会社のメンバー全員が同じことをできる(削除・契約確定を含む)。**この状態でよいと確認済み**
- **㉒ 会社設定を新設し、帳票テンプレートと単位を入れた。**単位は**明細のデータは文字列のまま**でサジェストだけ用意する形。**列1は畳むとユーザー管理が押せなくなるが許容**(案C)。**将来「畳んだ状態を覚える」改修を入れるときはこの判断を見直す**
- **⑳ テンプレートの説明シートは `設定カタログ`。**項目カタログから自動生成する。**「出力設定」という別概念があるため誤読の余地が残る**(シート名は定数1箇所なので再変更は安い)
- **⑲ `devenv.sh` は、起動時のコミットと現在のHEADを比べて、進んでいたらそのプロセスだけ起動し直す。****コミットしていない変更は検出できない**

### ⑱ 帳票出力(見積書のExcel出力) → **マージ済み(2026-08-20)**
提案書 `session-docs/growth-plan-18-document-output.md`(第4版・承認済み)、設計と罠は [Growth帳票出力](memory/growth-documents.md)。

- **見積詳細の「出力」→ 資料作成ドロワー → 見積書のExcelをダウンロード**まで通した。テンプレート管理画面(複数保持・既定指定・アップロード検査)も同じPRに入っている
- **今後の帳票と出力設定は、エンドポイントも画面も増やさずに追加できる形**にした
- **PDFは第2弾。**後から足せる形(生成物が複数ファイルを持てる構造・`?format=` のURL・出力設定の「形式」項目・テンプレートの印刷範囲)は第1弾に入っている
- **デプロイ時の注意が3つ**: ①マイグレーションが1本増えた ②**Asposeのライセンスを設定で渡さないと本番の起動が止まる** ③生成物の保存先の設定が要る
- **CIとローカルはライセンス無しで動く。**テストと開発環境は「評価版を許可する」設定で動かしており、**Secretsの登録は要らない**(PR本文に「CIにもSecretsが要る」と書いたのは誤り。2026-08-20 訂正済み)。ただし評価版で通ったテストは「透かしが入らないこと」の保証にはならない
- **小林さんの確認待ちが3つ**: 実際にExcelを開いての目視 / ライセンスを入れて透かしが消えること / 共通テンプレートのbase64同梱を実体の.xlsxに戻すか

### 顧客提示の準備(⑪) → **資料2本は作成済み。旧HTML3本の廃止は承認済み(2026-08-23)**
提案書 `session-docs/id-platform-plan-11-customer-demo-prep.md`。

- **認知用HTMLは2本立てで確定**(2026-08-19 小林さん判断): ① `design-docs/id-platform-overview.html`(しくみの概要) ② `design-docs/id-platform-screens-and-flow.html`(画面と操作の流れ)
- **旧HTML3本(ui-design-overview / schema-api-overview / dev-guidelines-overview)は廃止で確定(2026-08-23 小林さん判断)。**ただし**この3本は anyone-user-platform リポジトリの履歴に一度も存在しない**(2026-08-23 実測: `docs/design/` にあるのは新2本だけ)。**実体は Cowork の `design-docs/` 側にあるため、削除作業は Cowork のセッションで行う**
- **残り: 本番ビルドでの通し確認とデモ当日の手順書**

### 判断待ち・検討課題
1. ~~**404の文言が原因と無関係になる**~~ → **解消済み(2026-08-24、PR #27)。**ルート不在の404に `route_not_found` を割り当て、フロントの翻訳表に対応する分岐を足した。**業務上の「見積が見つからない」は従来どおり `not_found`。**両者を同じテストで固定してある
   - **誤検知の記録: この件を一度「解消済み」と誤って結論づけた(2026-08-24)。**バックエンドだけを見て「汎用文言だから直っている」と判断したため。実際は**フロントの翻訳表**が業務固有の文言へ訳していた。**文言の問題は、出す側と訳す側の両方を見るまで結論を出さない**
   - **B-8(会社切替で会社スコープの詳細URLへ戻され404になる)は未対応。**これが残っている限り、会社切替の直後に404そのものは出る。ただし文言は「指定されたURLは存在しません」になり、原因は読み取れるようになった
2. **登録済みメールアドレスの扱い**(アカウント列挙) → **GitHub Issue #67 に検討課題として記録済み**。見直しの引き金も書いてある
3. **判断1(登録済みメールの扱い)をADRとして残すことが未了**
4. 基盤とGrowthで**ログアウトの方式が違う**。揃えるかどうか
5. **旧フローの途中で止まっている利用者**(メール確認済み・パスワード未設定)への案内。検証環境には該当者がいないので未対応
6. **削除が確定した会社の所属行が Active のまま残る**(掃除の仕組みが無い)。退会の判定では数えない扱いにしたので実害は出ないが、名簿としては不正確なまま
7. **結合テストの実行順依存が、まだ他にも残っている**(⑰で変更フィードの3件は自前化して解消)。あるテストが共通データの会社名・部署名を書き換えたまま戻さず、別のテストがその名前を見ている。**いまは順番が合うので緑**だが、順序が変われば落ちる

### 手を動かせば済むもの
- **`tools/dev/devenv.sh` の積み残し3点**: ①`next-env.d.ts` でガードが止まる(**毎回起きる**。当面は `up --force` で回避) ②`down` が手起動のプロセスを止められない ③doctorの案内文
  - **④「更新しても反映されない」は ⑲ で直してPR提出済み**
- **用語統一の取りこぼし**: リポジトリ内に旧表記が残るが、**ほぼテストのメソッド名とコメント**で顧客には見えない。**画面には1件も残っていない**(2026-08-19 実機確認)
- デザイントークン生成スクリプトの出力ヘッダーに `npm run frontend:tokens` が残る
- **ログアウト中継ページの色がデザイントークンから切り離されている**(Growth側)
- **「確認メールを送りました」画面で、送信先のメールアドレスが語中で折り返す**(表示だけの問題。直すなら1行)
- **⑭(利用者の視点での抜け漏れ点検)の一覧を、⑮〜⑰でできるようになったことに合わせて更新する**。`session-docs/id-platform-plan-14-user-stories-and-gaps.md`

### 実装候補
[ID基盤の「後続候補」](memory/id-platform.md)と [Growthのv1積み残し](memory/growth.md)。
**Growth側はP1(権限別シナリオの確認・E2Eの自動化)が実質のリリース関門。**⑩-2(通し操作の自動テスト)と同じもので、**2リポジトリ共通の土台として設計したい**。**顧客提示で仕様が動く可能性があるため、提示後に着手する**(2026-08-19 判断)。

## 動作確認で確認済み

**証拠つきの記録は Cowork の `session-docs/verification-20260818-e2e.md` と `verification-20260819-plan15.md`。**個々の項目をここに書き写さない。

**通しで確認できている範囲**: 新しいサインアップ / 会社の削除の予約と取消 / 連携の撤回と再承認(二者同意) / アカウントの自己管理 / 基盤とGrowthの相互遷移とログアウト / 基盤コンソール11画面・Growth 4画面をPC幅とスマホ幅で巡回。

## 動作確認で見つかった不具合

**2026-08-21 に挙がった5件はすべて対応済みか承認済み。**残っているものは「次にやること」側で追う。

## Aspose.Cells 評価版は「1プロセスで開けるファイル100件」が上限【2026-08-21 実測】
**101件目で `You are using an evaluation copy and have opened files exceeding limitation.` が出る。**
- **`Growth.Documents.Tests` は現状およそ88〜90件を消費しており、残りは10〜14件しかない**
- ⑳の実装で素直にテストを足したら上限を超え、**既存テスト5本が巻き添えで落ちた**(共有フィールドでブックの開き直しを減らして解消)
- **今後このテストプロジェクトへExcelを開くテストを足すときは同じ壁に当たる。**恒久策はライセンス適用か、テストアセンブリの分割(プロセスが分かれれば各100件)

## 認知用HTMLは1本にまとめた【2026-08-21】
**2026-08-21 の計画5件(⑳㉑㉒㉓㉔)は、`design-docs/growth-fix-plan-20260821.html` に「タブで切り替える1枚もの」としてまとめてある(**㉔だけID基盤の件だが同じHTMLに入れている**)**(小林さん要望: 「いちいちファイルを切り替えて見るのが大変」)。
**件ごとのHTMLは作らない。**判断の正本は各件の md(`session-docs/`)。**mdを直したら、このHTMLを組み直す**(タブの中身はmdから作り直す。スタイルは既存HTMLを流用)。
